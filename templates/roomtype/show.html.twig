{% extends 'base.html.twig' %}

{% block title %}Roomtype{% endblock %}

{% block body %}
    <h1>Roomtype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ roomtype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ roomtype.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ roomtype.price }}</td>
            </tr>
        </tbody>
    </table>
    <div style="display: flex">
    <a style="margin-right: 10px" class="btn btn-primary" href="{{ path('roomtype_index') }}">back to list</a>

    <a style="margin-right: 10px" class="btn btn-secondary" href="{{ path('roomtype_edit', {'id': roomtype.id}) }}">edit</a>

    {{ include('roomtype/_delete_form.html.twig') }}
    </div>
{% endblock %}
