{% extends 'base.html.twig' %}

{% block title %}Paymentmethod{% endblock %}

{% block body %}
    <h1>Paymentmethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paymentmethod.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ paymentmethod.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paymentmethod_index') }}">back to list</a>

    <a href="{{ path('paymentmethod_edit', {'id': paymentmethod.id}) }}">edit</a>

    {{ include('paymentmethod/_delete_form.html.twig') }}
{% endblock %}
