{% extends 'base.html.twig' %}

{% block title %}Edit Paymentmethod{% endblock %}

{% block body %}
    <h1>Edit Paymentmethod</h1>

    {{ include('paymentmethod/_form.html.twig', {'button_label': 'Update'}) }}
    <div style="display: flex">
    <a style="margin-right: 10px" class="btn btn-primary" href="{{ path('paymentmethod_index') }}">back to list</a>

    {{ include('paymentmethod/_delete_form.html.twig') }}
    </div>
{% endblock %}
